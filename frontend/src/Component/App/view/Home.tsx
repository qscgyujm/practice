import React from 'react';
import styled from 'styled-components';

const HomeWrapper = styled.div`
  color: #ccF;
`;

const Home = () => (
  <HomeWrapper>
    Home
  </HomeWrapper>
);

export default Home;
